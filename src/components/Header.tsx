import { Logo } from '@/components/Logo';

const Header = () => {
  return (
    <header className="bg-base-border flex h-[300px] items-center justify-center">
      <Logo />
    </header>
  );
};

export { Header };
